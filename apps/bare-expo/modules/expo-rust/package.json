{
  "name": "expo-rust",
  "version": "0.0.1",
  "private": true,
  "description": "Example Rust-based Expo module for bare-expo",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "author": "Expo",
  "homepage": "https://expo.dev",
  "scripts": {
    "build:rust": "cargo build --release",
    "build:rust:android": "cargo ndk -t armeabi-v7a -t arm64-v8a -t x86 -t x86_64 build --release",
    "build:rust:ios": "cargo build --release --target aarch64-apple-ios && cargo build --release --target x86_64-apple-ios",
    "clean": "cargo clean"
  },
  "peerDependencies": {
    "expo": "workspace:*",
    "expo-modules-core": "*"
  }
}
